name: helloworld-configurable
summary: 'a hello world web server'
description: |
    This example charm runs a web service that
    always returns the string "hello, world" by default,
    but the message can be configured.
